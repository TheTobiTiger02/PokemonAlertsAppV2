<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Optional: allow "Allow all the time" so background jobs can obtain a fresh location fix -->
    <uses-permission
        android:name="android.permission.ACCESS_BACKGROUND_LOCATION"
        tools:targetApi="29" />
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />
    <!-- Lets an active "I'm going" journey use Android 16+ Live Update surfaces. -->
    <uses-permission
        android:name="android.permission.POST_PROMOTED_NOTIFICATIONS"
        tools:targetApi="36" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:targetApi="31" />

    <application
        android:name=".PokemonAlertsApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PokemonAlertsV2"
        tools:targetApi="31">
        <activity
            android:name=".ui.alerts.AlertDetailActivity"
            android:exported="true"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:theme="@style/Theme.PokemonAlertsV2"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".ui.counters.ManualRaidActivity"
            android:exported="false"
            android:theme="@style/Theme.PokemonAlertsV2" />
        <activity
            android:name=".ui.settings.SpeciesSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.PokemonAlertsV2"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".ui.godex.GoDexLoginActivity"
            android:exported="false"
            android:theme="@style/Theme.PokemonAlertsV2" />
        <activity
            android:name=".ui.auth.PokebattlerLoginActivity"
            android:exported="false"
            android:label="Sign in to Pokébattler"
            android:theme="@style/Theme.PokemonAlertsV2" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                Deep links onto the same destinations the notification/widget
                EXTRA_INITIAL_TAB extras already reach, so the app is linkable from
                outside: pokemonalerts://alerts | history | map | settings[/<page>]
                | alert/<id>
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="pokemonalerts" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/csv" />
                <data android:mimeType="text/comma-separated-values" />
                <data android:mimeType="application/csv" />
                <data android:mimeType="application/vnd.ms-excel" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/csv" />
                <data android:mimeType="text/comma-separated-values" />
                <data android:mimeType="application/csv" />
                <data android:mimeType="application/vnd.ms-excel" />
            </intent-filter>
        </activity>
        <service
            android:name=".fcm.PokemonFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name=".tracking.ArrivalTrackingService"
            android:exported="false"
            android:foregroundServiceType="location" />
        <!--
            Drives the post-arrival raid Live Update: refresh alarms and the notification's
            Dismiss action both land here.
        -->
        <receiver
            android:name=".raidwatch.RaidWatchReceiver"
            android:exported="false" />
        <!--
            The Live Update's "Copy team" action. Invisible and finishes immediately, so the
            trainer stays in Pokémon GO; an Activity rather than a receiver because a
            background app cannot reliably write the clipboard from Android 10 onward.
        -->
        <activity
            android:name=".raidwatch.CopyRaidTeamActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <receiver
            android:name=".notifications.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.pokemonalertsv2.ACTION_DISMISS_ALERT" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".widget.WidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.PokemonAlertsV2"
            android:label="@string/widget_config_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".widget.AlertsWidgetProvider"
            android:exported="true"
            android:label="@string/widget_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.example.pokemonalertsv2.widget.ACTION_REFRESH" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.example.pokemonalertsv2.widget.ACTION_TIMER_TICK" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.example.pokemonalertsv2.widget.ACTION_DISMISS_WIDGET" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.example.pokemonalertsv2.widget.ACTION_NAVIGATE" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.example.pokemonalertsv2.widget.ACTION_ITEM_CLICK" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/alerts_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.NearbyRadarWidgetProvider"
            android:exported="true"
            android:label="@string/widget_radar_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.example.pokemonalertsv2.widget.ACTION_REFRESH_RADAR" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.example.pokemonalertsv2.widget.ACTION_TIMER_TICK_RADAR" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/nearby_radar_widget_info" />
        </receiver>

        <service
            android:name=".widget.AlertsWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/maps_api_key" />
        
        <!-- FileProvider for sharing alert HTML files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
