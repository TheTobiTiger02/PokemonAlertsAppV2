<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root_pair"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@drawable/widget_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <LinearLayout
            android:id="@+id/header_open_app_pair"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/img_logo_pair"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_marginEnd="8dp"
                android:src="@drawable/ic_widget_pokeball"
                android:contentDescription="@string/widget_logo_cd" />

            <TextView
                android:id="@+id/tv_title_pair"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_title"
                android:textStyle="bold"
                android:textSize="16sp"
                android:textColor="@color/widget_on_surface" />
        </LinearLayout>

        <TextView
            android:id="@+id/tv_count_pair"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="11sp"
            android:textStyle="bold"
            android:textColor="@color/widget_primary"
            android:layout_marginEnd="4dp" />

        <ImageButton
            android:id="@+id/btn_refresh_pair"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/widget_icon_button_background"
            android:contentDescription="@string/refresh_alerts"
            android:tint="@color/widget_primary"
            android:src="@drawable/ic_refresh"
            android:padding="12dp" />
    </LinearLayout>

    <include layout="@layout/widget_alert_pair_first" />

    <include layout="@layout/widget_alert_pair_second" />
</LinearLayout>
