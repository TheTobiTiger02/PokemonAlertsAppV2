Make the "Copy for GO" search string emit `@Dynama` instead of `@Dynamax Cannon` for the move Dynamax Cannon.

## Change 1 — `app/src/main/java/com/example/pokemonalertsv2/data/counters/PokemonGoSearch.kt`

Modify the private `asMoveToken()` extension (lines 90–93), which currently prepends `@` to the raw move name:
- If the move name starts with "Dynamax" (case-insensitive; compare after normalizing `_` to a space so the raw Pokébattler id `DYNAMAX_CANNON` is covered too), emit `@Dynama` instead of `@<name>`.
- All other moves keep the current `@<name>` behavior.
- Add a brief KDoc/comment explaining the constraint: `dynamax` is a reserved search keyword in Pokémon GO (it returns Dynamax-capable Pokémon), so the move "Dynamax Cannon" is searched by its shortest safe prefix `@Dynama`, which still prefix-matches the move.

This is the single generation point for these tokens — the counters screen "Copy for GO" button and the raid notification "Copy team" action both go through `PokemonGoSearch.teamQuery`, so both consumers get fixed automatically.

## Change 2 — update pinned test expectations

Two tests assert the full query string containing `@Dynamax Cannon`:
- `app/src/test/java/com/example/pokemonalertsv2/data/counters/PokemonGoSearchTest.kt:195` (JVM unit test) — change `@Dynamax Cannon` to `@Dynama` in the expected string.
- `app/src/androidTest/java/com/example/pokemonalertsv2/ui/counters/PokemonGoSearchClipboardComposeTest.kt:153` (instrumented test) — same change.
- Add one focused unit test in `PokemonGoSearchTest.kt` documenting the behavior: a team with a "Dynamax Cannon" copy yields `@Dynama` in the charge-move group (also covering the `DYNAMAX_CANNON` raw-id variant).

No other files reference this move name; fixtures at the test lines that build the Eternatus slot stay as-is.

## Verification

Run the JVM unit tests: `./gradlew.bat :app:testDebugUnitTest --tests "com.example.pokemonalertsv2.data.counters.PokemonGoSearchTest"`. The instrumented compose test requires a connected device/emulator; I'll note it as updated-but-not-run if none is available.